# OGMA - Installation Complète GPU NVIDIA + Audio Avancé
# ========================================================
# Tout inclus : standard CPU + CUDA + Whisper VAD + librosa + soundfile.
#
# Usage : pip install -r requirements/requirements-full-nvidia-audio.txt
# Prérequis : CUDA 12.8 + drivers NVIDIA 581+
# ========================================================

# Index PyTorch CUDA 12.8 (doit être en premier)
--extra-index-url https://download.pytorch.org/whl/cu128
--extra-index-url https://jllllll.github.io/llama-cpp-python-cuBLAS-wheels/AVX2/cu128

# --- Core OGMA (standard CPU) ---
-r ../requirements.txt

# --- Surcharge GPU (remplace torch CPU) ---
-r requirements-nvidia.txt

# --- Audio avancé (VAD, librosa, soundfile, TTS cloud premium) ---
-r requirements-audio.txt
