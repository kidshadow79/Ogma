# OCTOPUS v1.8.8 - Requirements
# Interface utilisateur
gradio>=5.42.0

# Requests HTTP et API
requests>=2.31.0

# Manipulation de données
pandas>=2.0.0
numpy>=1.24.0

# Traitement d'images et vision
opencv-python>=4.8.0
Pillow>=10.0.0

# Traitement de documents
pypdf>=3.0.0
python-docx>=0.8.11

# Support GGUF/LlamaCpp (optionnel - peut être installé manuellement)
llama-cpp-python>=0.2.0

# Utilitaires Python standards (déjà inclus dans Python)
# pathlib (Python 3.4+)
# json (standard library)
# datetime (standard library)
# uuid (standard library)
# asyncio (Python 3.4+)
# threading (standard library)
# queue (standard library)
# traceback (standard library)
# functools (standard library)
# re (standard library)
# shutil (standard library)
# os (standard library)
# time (standard library)
# urllib.parse (standard library)
# mimetypes (standard library)
# base64 (standard library)

# Notes d'installation :
# - llama-cpp-python peut nécessiter une compilation spécifique selon votre matériel
# - Pour GPU NVIDIA : pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu124
# - Pour CPU uniquement : pip install llama-cpp-python
# - opencv-python peut être remplacé par opencv-python-headless pour les serveurs